#include<stdio.h>
int main()
{

    // char ch;
    // putchar(ch);
    // scanf("%s", &ch);

    // int a, b;
    
    // // 其中的3可以进行约束，三个后给后边，但是这样只能约束整型
    // scanf("%3d%d", &b, &a);
    
    // printf("%d\n", a);
    // printf("%d\n", b);


    char ch;
    // 这样只会接收一个
    ch = getchar();
    putchar(ch);
    
    
    return 0;
}